<template>
  <div class="page-wrapper">
    <!-- <div if="{{!result.length}}" class="no-data">
      <image class="no-data-img" src="/assets/images/no-data.png"></image>
      <text class="text-remark-big">暂无数据</text>
    </div> -->
    <list class="list-box">
      <list-item for="{{list}}">
        <text class="date">2020020</text>
        <div class="item-container">
          <image class="img" src="../../assets/images/collect-item.png"></image>
          <div class="item-box" style="flex-direction: column; align-items: flex-start;">
            <text class="item-t">{{$item.title | ellipsisA}}</text>
          </div>
        </div>

      </list-item>
    </list>
  </div>
</template>
<script>
import router from '@system.router'
export default {
  private: {
    list: [
      {
        id: "1",
        title: "花瓣网-陪你做生活的设计师花瓣网",
      },
      {
        id: "2",
        title: "花瓣网-陪你做生活的设计师",
      
      },
      {
        id: "3",
        title: "花瓣网-陪你做生活的设计师",
     
      },
    ]
  },
  ellipsisA(value) {
    if (!value) return ''
    if (value.length > 20) {
      return value.slice(0, 20) + '...'
    }
    return value
  }
}
</script>

<style lang="less">
@import '../../assets/styles/index.less';
.page-wrapper {
  background-color: #fff;
}
.no-data {
  flex: 1;
  .flex-box-mixins(column, center, center);
}
.no-data-img {
  width: 200px;
  margin-bottom: @gap-2;
}
.list-box {
  padding: 20px 30px;
  list-item {
    padding: 15px 0;
    margin-bottom: 10px;
    border-bottom: 1px solid #eee;
    flex-direction: column;
    .date {
    width: 250px;
    height: 68px;
    background-color: #f7f8fb;
    border-radius: 44px;
    text-align: center;
    margin: 20px 0;
  }
    .item-container {
      .item-box {
        width: 80%;
      }
      .item-t {
        height: 40px;
        width: 100%;
        font-size: 30px;
        color: #000;
        text-overflow: ellipsis;
        margin-bottom: 10px;
      }
      .img {
        width: 60px;
        height: 60px;
        align-items: center;
        background: #567aff;
        margin-right: 35px;
      }
    }
    .check-box {
      margin-top: 10px;
      align-items: center;
      width: 40px;
      height: 40px;
      border: 2px solid #ccc;
      border-radius: 50%;
    }
    .check-active {
      width: 40px;
      height: 40px;
      background-image: url('../../assets/images/check.png');
      background-position: center;
    }
  }
}
</style>
